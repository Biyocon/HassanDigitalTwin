# Brian Tracy Skill

This skill folder is reserved for the Brian Tracy skill content. Add prompt templates, configuration files, or supporting assets for the Brian Tracy workflow here so future uploads can live under `skills/brian-tracy/`.

**Expected files (examples):**
- `prompt.md` for the core prompt or persona text.
- `config.json` or `config.yaml` for structured settings.
- `examples/` for sample inputs and outputs.
